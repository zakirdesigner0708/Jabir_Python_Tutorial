# Sequence of denominators
sequence = [4, 10]

# Loop through each denominator in the sequence
for denom in sequence:
    decimal_value = 1 / denom
    print(f"The decimal equivalent of 1/{denom} is: {decimal_value}")
